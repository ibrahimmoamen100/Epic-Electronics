# نظام أولوية عرض المنتجات

## 📋 الوصف

تم إضافة نظام لتخصيص ترتيب عرض المنتجات في صفحة المنتجات (`/products`) بدلاً من الاعتماد فقط على تاريخ الإنشاء. يتيح هذا النظام للمسؤول تحديد أولوية ظهور المنتجات بشكل يدوي.

## ✨ الميزات

### 1. **حقل أولوية الظهور (Display Priority)**
- تم إضافة حقل اختياري `displayPriority` إلى نموذج المنتج
- الأرقام الأقل لها أولوية أعلى (مثلاً: 1 يظهر قبل 10)
- المنتجات بدون أولوية محددة تظهر بعد المنتجات ذات الأولوية

### 2. **إدارة الأولوية من لوحة التحكم**
- عمود جديد في جدول المنتجات يعرض أولوية الظهور
- إمكانية تعديل الأولوية مباشرة من الجدول:
  - انقر على الأولوية لتعديلها
  - أدخل رقم جديد (0 أو أكبر)
  - اضغط Enter أو زر ✓ للحفظ
  - اضغط Esc أو زر ✕ للإلغاء
  - اترك الحقل فارغاً لإزالة الأولوية

### 3. **منطق الفرز التلقائي**
يتم ترتيب المنتجات في صفحة `/products` كالتالي:

1. **إذا اختار المستخدم فلتر فرز** (السعر، الاسم):
   - يتم استخدام الفلتر المحدد
   
2. **إذا لم يختر المستخدم فلتر** (افتراضي):
   - أولاً: فرز حسب `displayPriority` (تصاعدي)
   - ثانياً: المنتجات بدون أولوية تأتي بعد المنتجات ذات الأولوية
   - ثالثاً: المنتجات بنفس الأولوية (أو بدون أولوية) تُفرز حسب تاريخ الإنشاء (الأحدث أولاً)

## 🔧 التعديلات التقنية

### الملفات المعدلة:

1. **`src/types/product.ts`**
````typescript
displayPriority: z.number().optional(), // Priority for display ordering
````

2. **`src/pages/Products.tsx`**
```typescript
// Default sorting logic
const aPriority = a.displayPriority ?? Number.MAX_SAFE_INTEGER;
const bPriority = b.displayPriority ?? Number.MAX_SAFE_INTEGER;

if (aPriority !== bPriority) {
  return aPriority - bPriority;
}

// If both have same priority, sort by creation date
const aDate = new Date(a.createdAt || 0).getTime();
const bDate = new Date(b.createdAt || 0).getTime();
return bDate - aDate;
```

3. **`src/components/ProductTable.tsx`**
- إضافة عمود "أولوية الظهور"
- إضافة واجهة تعديل مباشرة للأولوية
- إضافة `onUpdatePriority` prop

4. **`src/pages/Admin.tsx`**
- إضافة دالة `handleUpdatePriority`
- تمرير الدالة إلى `ProductTable`

## 📖 كيفية الاستخدام

### من لوحة التحكم:

1. اذهب إلى لوحة التحكم (`/admin`)
2. في جدول المنتجات، ستجد عمود "أولوية الظهور"
3. انقر على القيمة الحالية (أو "بدون" إذا لم تكن محددة)
4. أدخل رقم الأولوية المطلوب:
   - **1-10**: أولوية عالية جداً (للمنتجات المميزة)
   - **11-50**: أولوية متوسطة
   - **51+**: أولوية منخفضة
   - **فارغ**: بدون أولوية (سيظهر في النهاية)
5. اضغط Enter أو زر ✓ للحفظ

### للمطورين:

#### تحديث منتج بأولوية:
```typescript
await updateProduct({
  ...product,
  displayPriority: 5 // رقم الأولوية
});
```

#### إزالة الأولوية:
```typescript
await updateProduct({
  ...product,
  displayPriority: undefined
});
```

## 🎯 أمثلة عملية

### مثال 1: ترويج منتج جديد
- المنتج الحالي: `displayPriority = undefined` (يظهر حسب التاريخ)
- بعد التعديل: `displayPriority = 1` (يظهر أولاً)

### مثال 2: ترتيب العروض الخاصة
- منتج العرض الأول: `displayPriority = 1`
- منتج العرض الثاني: `displayPriority = 2`
- منتج العرض الثالث: `displayPriority = 3`

### مثال 3: مزيج من المنتجات
الترتيب النهائي في الصفحة:
1. منتج A (priority: 1)
2. منتج B (priority: 5)
3. منتج C (priority: 5، تاريخ: 2026-02-01)
4. منتج D (priority: 5، تاريخ: 2026-01-30)
5. منتج E (priority: 10)
6. منتج F (بدون priority، تاريخ: 2026-02-02)
7. منتج G (بدون priority، تاريخ: 2026-02-01)

## ⚠️ ملاحظات مهمة

1. **عدم التعارض مع الفلاتر**: نظام الأولوية يعمل فقط عندما لا يختار المستخدم فلتر فرز محدد
2. **القيم السالبة**: غير مسموح بها (الحد الأدنى هو 0)
3. **التحديثات**: التغييرات تظهر فوراً في صفحة المنتجات
4. **Firebase**: حقل `displayPriority` اختياري ولا يؤثر على المنتجات القديمة

## 🔍 استكشاف الأخطاء

### المنتج لا يظهر في الترتيب المتوقع؟
1. تأكد من عدم وجود فلتر فرز مفعل في صفحة المنتجات
2. تحقق من قيمة `displayPriority` في لوحة التحكم
3. قم بتحديث الصفحة (F5)

### لا يمكن حفظ الأولوية؟
1. تأكد من أن القيمة رقم صحيح >= 0
2. تحقق من صلاحيات Firebase
3. راجع console للأخطاء

## 📊 إحصائيات

- **التعقيد**: متوسط (5-7/10)
- **الملفات المعدلة**: 4
- **الأسطر المضافة**: ~150
- **التوافق**: كامل مع النظام الحالي
