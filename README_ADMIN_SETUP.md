# 🔐 نظام إدارة المتجر - دليل الإعداد

## نظرة عامة
نظام إدارة متجر إلكتروني مع نظام مصادقة آمن باستخدام Firebase و localStorage كاحتياطي.

## 🚀 الإعداد السريع

### 1. تثبيت المتطلبات
```bash
npm install
```

### 2. إعداد Firebase
1. أنشئ مشروع جديد في [Firebase Console](https://console.firebase.google.com/)
2. فعّل Firestore Database
3. انسخ إعدادات المشروع إلى `src/lib/firebase.ts`

### 3. تحديث قواعد Firebase
```javascript
rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
```

### 4. تهيئة الإدارة
```bash
npm run dev
```
- اذهب إلى `/admin`
- اضغط "تهيئة إعدادات الإدارة (للمطورين)" (يظهر في وضع التطوير فقط)
- كلمة المرور الافتراضية: `45086932`

## 🔧 الميزات

### نظام المصادقة
- ✅ كلمة مرور آمنة محفوظة في Firebase
- ✅ نظام احتياطي مع localStorage
- ✅ جلسات محمية مع انتهاء صلاحية
- ✅ تسجيل دخول آمن

### واجهة الإدارة
- ✅ لوحة تحكم شاملة
- ✅ إدارة المنتجات
- ✅ إحصائيات المبيعات
- ✅ نظام الكاشير
- ✅ تحليل الأرباح

## 🔐 كلمة المرور الافتراضية
```
45086932
```

## 🛠️ تخصيص كلمة المرور
عدّل الملف `src/lib/adminAuth.ts`:
```typescript
private readonly ADMIN_PASSWORD = 'YOUR_NEW_PASSWORD';
```

## 📁 هيكل المشروع
```
src/
├── lib/
│   ├── adminAuth.ts      # نظام المصادقة
│   └── firebase.ts       # إعدادات Firebase
├── components/
│   └── AdminLogin.tsx    # واجهة تسجيل الدخول
├── hooks/
│   └── useAdminAuth.ts   # Hook المصادقة
└── pages/
    └── Admin.tsx         # صفحة الإدارة
```

## 🔄 نسخ المشروع
1. انسخ المجلد
2. عدّل `firebase.ts` بإعدادات المشروع الجديد
3. شغّل `npm run dev`
4. اضغط زر التهيئة
5. جاهز للاستخدام!

## ⚠️ ملاحظات أمنية
- قواعد Firebase مفتوحة للتطوير - قم بتقييدها للإنتاج
- كلمة المرور محفوظة في Firebase - تأكد من أمان المشروع
- النظام يعمل مع localStorage كاحتياطي

## 📞 الدعم
للمساعدة في الإعداد أو التخصيص، راجع ملف `PROJECT_SETUP_GUIDE.md`
